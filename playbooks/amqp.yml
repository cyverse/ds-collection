---
- name: Set up RabbitMQ broker
  hosts: amqp
  become: true
  roles:
    # This is disabled until DS-821 is started
    # - role: cyverse.ds.firewalld
    #   vars:
    #     firewalld_service_name: amqp
    #     firewalld_service_ports: "{{ _amqp_broker_port }}"
    #     firewalld_admin_hosts: "{{ _admin_hosts }}"
    #     firewalld_system_hosts:
    #       - "{{ groups['irods_catalog'] }}"
    #       - "{{ groups['webdav'] }}"
    #     firewalld_external_hosts: "{{ _amqp_external_clients }}"

    - role: cyverse.ds.rabbitmq
      vars:
        rabbitmq_admin_username: "{{ _amqp_admin_username }}"
        rabbitmq_admin_password: "{{ _amqp_admin_password }}"
        rabbitmq_broker_port: "{{ _amqp_broker_port }}"
        rabbitmq_mgmt_port: "{{ _amqp_management_port }}"
