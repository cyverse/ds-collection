---
- name: Test infra_network
  hosts: all
  tasks:
    - name: Test template expansion of txqueuelen.rules.yaml
      ansible.builtin.debug:
        msg: 'TODO: implement for {{ item }}'
      loop:
        - ansible_managed
        - rule

    - name: Test plays
      ansible.builtin.debug:
        msg: 'TODO: implement'